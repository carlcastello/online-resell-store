export interface IOwnProps {
  match: {
    params: {
      id: string,
    }
  },
  classes: {
    container: string,
    descriptionFooter: string,
    paragraph: string
  }
}

export interface IReduxProps {
  
}